/**
 * A Loading page is used when you want to show a specific UI and/or massage to the user while the data is being fetched.
 * It will be rendered when a loading state is happening in the folder page and in its children (for example, the news/[id] page).
 */
const NewsListLoadingPage = () => {
  return <p>Loading news for you...</p>
}

export default NewsListLoadingPage
