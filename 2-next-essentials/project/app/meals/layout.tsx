import { FC, ReactNode } from 'react'

/**
 * This layout at Meals-page level will be wrapped by root's layout aswell
 */
const MealsLayout: FC<{ children: ReactNode }> = ({ children }) => {
  return children
}

export default MealsLayout
